// Minimum hit area for touch-friendly controls (44x44px, per common mobile
// accessibility guidance). Compose with `cn()` alongside other classes.
export const MOBILE_TOUCH_TARGET = "min-w-[44px] min-h-[44px]";
